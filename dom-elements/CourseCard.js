export class CourseCard {
    constructor(courseNode) {
        this.courseNode = courseNode
        // holds the dom element representing the courseNode
        this.element = document.createElement("div")
    }

    // to implement: makeDomElement()
}